<h1>Create New</h1>
  <%= form_for @dog do |f| %> <!-- Create form for user object -->
    Name: <%= f.text_field :name %>
    Age: <%= f.text_field :age %>
    <%= f.submit %> <!-- Create submit button-->
  <% end %>
